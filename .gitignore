# Models are large binaries fetched by scripts/setup.py
models/
# Meeting transcripts are sensitive by definition
transcripts/
eval/tmp/
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# The firm glossary is tracked so a clone is immediately usable.
# terms.example.yaml holds invented terms and is what the tests read.
# Import scratch files stay local.
glossary/candidates.yaml
